<UserControl x:Class="Universal_x86_Tuning_Utility.Views.Controls.SensorsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             FlowDirection="LeftToRight">

    <ui:Card Padding="18">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- CPU section -->
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="SensorLabel" />
                    <ColumnDefinition Width="*" SharedSizeGroup="SensorBar" />
                    <ColumnDefinition Width="70" SharedSizeGroup="SensorValue" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                           Margin="0,0,0,8"
                           FontSize="18"
                           FontWeight="Medium"
                           Text="CPU" />

                <Label Grid.Row="1" Grid.Column="0"
                       VerticalContentAlignment="Center"
                       Content="Utilization"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ProgressBar x:Name="_cpuUsageBar"
                             Grid.Row="1" Grid.Column="1"
                             Margin="16,0,16,0"
                             Minimum="0" Maximum="100" Value="0" />
                <Label x:Name="_cpuUsageLabel"
                       Grid.Row="1" Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       VerticalContentAlignment="Center"
                       Content="--"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <Label Grid.Row="2" Grid.Column="0"
                       VerticalContentAlignment="Center"
                       Content="Temperature"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ProgressBar x:Name="_cpuTempBar"
                             Grid.Row="2" Grid.Column="1"
                             Margin="16,0,16,0"
                             Minimum="0" Maximum="100" Value="0" />
                <Label x:Name="_cpuTempLabel"
                       Grid.Row="2" Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       VerticalContentAlignment="Center"
                       Content="--"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <Label Grid.Row="3" Grid.Column="0"
                       VerticalContentAlignment="Center"
                       Content="Power"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ProgressBar x:Name="_cpuPowerBar"
                             Grid.Row="3" Grid.Column="1"
                             Margin="16,0,16,0"
                             Minimum="0" Maximum="300" Value="0" />
                <Label x:Name="_cpuPowerLabel"
                       Grid.Row="3" Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       VerticalContentAlignment="Center"
                       Content="--"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <Label Grid.Row="4" Grid.Column="0"
                       VerticalContentAlignment="Center"
                       Content="Clock"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ProgressBar x:Name="_cpuClockBar"
                             Grid.Row="4" Grid.Column="1"
                             Margin="16,0,16,0"
                             Minimum="0" Maximum="6000" Value="0" />
                <Label x:Name="_cpuClockLabel"
                       Grid.Row="4" Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       VerticalContentAlignment="Center"
                       Content="--"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </Grid>

            <!-- GPU section -->
            <Grid Grid.Column="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="SensorLabel" />
                    <ColumnDefinition Width="*" SharedSizeGroup="SensorBar" />
                    <ColumnDefinition Width="70" SharedSizeGroup="SensorValue" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                           Margin="0,0,0,8"
                           FontSize="18"
                           FontWeight="Medium"
                           Text="GPU" />

                <Label Grid.Row="1" Grid.Column="0"
                       VerticalContentAlignment="Center"
                       Content="Utilization"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ProgressBar x:Name="_gpuUsageBar"
                             Grid.Row="1" Grid.Column="1"
                             Margin="16,0,16,0"
                             Minimum="0" Maximum="100" Value="0" />
                <Label x:Name="_gpuUsageLabel"
                       Grid.Row="1" Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       VerticalContentAlignment="Center"
                       Content="--"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <Label Grid.Row="2" Grid.Column="0"
                       VerticalContentAlignment="Center"
                       Content="Temperature"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ProgressBar x:Name="_gpuTempBar"
                             Grid.Row="2" Grid.Column="1"
                             Margin="16,0,16,0"
                             Minimum="0" Maximum="100" Value="0" />
                <Label x:Name="_gpuTempLabel"
                       Grid.Row="2" Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       VerticalContentAlignment="Center"
                       Content="--"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <Label Grid.Row="3" Grid.Column="0"
                       VerticalContentAlignment="Center"
                       Content="Power"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ProgressBar x:Name="_gpuPowerBar"
                             Grid.Row="3" Grid.Column="1"
                             Margin="16,0,16,0"
                             Minimum="0" Maximum="300" Value="0" />
                <Label x:Name="_gpuPowerLabel"
                       Grid.Row="3" Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       VerticalContentAlignment="Center"
                       Content="--"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <Label Grid.Row="4" Grid.Column="0"
                       VerticalContentAlignment="Center"
                       Content="Clock"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ProgressBar x:Name="_gpuClockBar"
                             Grid.Row="4" Grid.Column="1"
                             Margin="16,0,16,0"
                             Minimum="0" Maximum="3000" Value="0" />
                <Label x:Name="_gpuClockLabel"
                       Grid.Row="4" Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       VerticalContentAlignment="Center"
                       Content="--"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </Grid>
        </Grid>
    </ui:Card>
</UserControl>
